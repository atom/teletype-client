syntax = "proto3";

message NetworkMessage {
  string network_id = 1;

  oneof variant {
    StarConnection star_connection = 2;
    StarBroadcast star_broadcast = 3;
  }

  message StarConnection {}

  message StarBroadcast {
    string sender_id = 1;
    bytes body = 2;
  }
}
